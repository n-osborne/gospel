(ocamllex
 (modules pps ulexer))

(menhir
 (modules uparser)
 (flags
  (-v --explain)))

(library
 (name gospel_libs)
 (public_name gospel.gospel_libs)
 (preprocess
  (pps ppx_deriving.show))
 (libraries
  ocaml-compiler-libs.shadow
  fmt
  findlib
  ppxlib
  ppxlib.ast
  pp_loc
  inferno)
 (modules_without_implementation id_uast parse_uast))

(rule
 (alias fmt)
 (enabled_if %{bin-available:ex})
 (action
  (progn
   (run
    ex
    -u
    NONE
    -n
    %{dep:pps.mll}
    -c
    "g/^{/+,/^}$/-!ocamlformat --impl -m 78 - | sed 's/^./  &/'"
    -c
    "g/ {$/+,/^    }$/-!ocamlformat --impl -m 74 - | sed 's/^./      &/'"
    -c
    "wq! pps.formatted.mll")
   (diff? pps.mll pps.formatted.mll))))
